name: 'Download and preprocess'
description: |
  Download data and preprocess
inputs:
        - {name: tf_data_dir, description: 'Data Directory'}
outputs:
        - {name: tf_data_dir, description: 'something'}
implementation:
  container:
    image: docker.io/petehwu/download:latest 
    command: ['python']
    args: [
      download.py,
      --tf_data_dir, {inputValue: tf_data_dir},
    ]
